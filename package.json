{
  "name": "vralle-lazyload",
  "version": "0.9.6",
  "description": "Lazy loading images to speed up loading pages and reduce the load on the server. Images are loaded when they get to the screen. Uses lazysizes.js",
  "private": true,
  "author": "vralle",
  "license": "MIT",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "copy-lazysizes": "shx mkdir -p vendor/lazysizes/ && shx cp node_modules/lazysizes/*.js vendor/lazysizes/",
    "copy-lazysizes-plugins": "shx mkdir -p vendor/lazysizes/plugins/ && shx cp -r node_modules/lazysizes/plugins/ vendor/lazysizes/",
    "copy-picturefill": "shx mkdir -p vendor/picturefill/ && shx cp -r node_modules/picturefill/dist/ vendor/picturefill/",
    "dist": "npm-run-all copy-*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "lazysizes": "^4.1.8",
    "picturefill": "^3.0.3"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "shx": "^0.3.2"
  }
}
