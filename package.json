{
  "name": "vralle-lazyload",
  "version": "0.8.1",
  "description": "Lazy loading images to speed up loading pages and reduce the load on the server. Images are loaded when they get to the screen. Uses lazysizes.js",
  "private": true,
  "author": "vralle",
  "license": "MIT",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "vendor": "shx cp node_modules/lazysizes/lazysizes.js vendor/lazysizes/  && shx cp node_modules/lazysizes/lazysizes.min.js vendor/lazysizes && shx cp -R node_modules/lazysizes/plugins/ vendor/lazysizes/",
    "dist": "npm-run-all vendor",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "lazysizes": "^4.0.4"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "shx": "^0.3.1"
  }
}
